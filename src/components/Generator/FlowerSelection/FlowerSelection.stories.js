import FlourSelection from "./FlourSelection";

export default {
  title: "Pages/FlourSelection",
  component: FlourSelection,
};

const Template = (args) => <FlourSelection {...args} />;

export const Primary = Template.bind({});
Primary.args = {};
